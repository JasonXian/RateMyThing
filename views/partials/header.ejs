<!DOCTYPE html>
<html>
    <head>
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>Rate My Thing</title>
        <link rel="stylesheet" href="/stylesheets/bootstrap.min.css"></link>
        <link rel="stylesheet" href="/stylesheets/main.css"></link>
        <link href="/stylesheets/bootstrap-glyphicons.css" rel="stylesheet"></link>
        <script src="https://use.fontawesome.com/58b9791761.js"></script>
    </head>
    <body>
        <nav class="navbar navbar-default navbar-fixed-top">
            <div class="container">
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                      <span class="sr-only">Toggle navigation</span>
                      <span class="icon-bar"></span>
                      <span class="icon-bar"></span>
                      <span class="icon-bar"></span>
                    </button>
                    <a class="navbar-brand" href="/">Rate My Thing</a>
                </div>
                <div id="navbar" class="collapse navbar-collapse">
                  <ul class="nav navbar-nav">
                    <li class="<%= typeof page !== 'undefined' && page === 'things' ? 'active' : '' %>"><a href="/things">Home</a></li>
                    <li>
                        <a href="https://github.com/JasonXian/RateMyThing" target="_blank" style="padding: 15px 0 0 15px;">
                            <i class="fa fa-github" aria-hidden="true" style="color: white; font-size: 30px;"></i>
                        </a>
                    </li>
                  </ul>
                  <ul class="nav navbar-nav navbar-right">
                      <% if(!currentUser){ %>
                          <li class="<%= typeof page !== 'undefined' && page === 'login' ? 'active' : '' %>"><a href="/login">Login</a></li>
                          <li class="<%= typeof page !== 'undefined' && page === 'register' ? 'active' : '' %>"><a href="/register">Register</a></li>
                      <% } else { %>
                          <li><a href="/users/<%= currentUser.id %>"><%= currentUser.username %></a></li>
                          <li><a href="/logout">Logout</a></li>
                      <% } %>
                  </ul>
                </div>
            </div>
        </nav>
        <div id="headerPadding"></div>
        <div class="container">
            <% if(error && error.length > 0) { %>
                <div class="alert alert-danger" role="alert">
                    <%= error %>
                </div>
            <% } %>
            <% if(success && success.length > 0) { %>
                <div class="alert alert-success" role="alert">
                    <%= success %>
                </div>
            <% } %>
        </div>
        <div class="container">